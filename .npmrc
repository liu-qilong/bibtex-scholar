tag-version-prefix=""
# Avoid ERESOLVE on optional peers (vitest/vite vs @types/node) for plain `npm install`.
# Note: the production build is `npm run build`, not `npm install build`.
legacy-peer-deps=true
